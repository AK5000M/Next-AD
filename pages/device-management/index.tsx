import React from "react";
import DeviceManagementContent from "@/page-contents/device-management";

const DeviceManagement = () => {
	return (
		<React.Fragment>
			<DeviceManagementContent />
		</React.Fragment>
	);
};

export default DeviceManagement;
